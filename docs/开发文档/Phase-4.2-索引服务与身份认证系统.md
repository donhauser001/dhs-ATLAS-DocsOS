# Phase 4.2 - 索引服务与身份认证系统

> 版本: 1.0.0  
> 创建日期: 2026-01-05  
> 状态: 📋 规划中

## 概述

### 核心理念

> **"文档即数据，索引即数据库"**
> 
> **"用户自建的文档，可以随便乱丢，索引服务会帮他整理好非常正规且专业的数据格式"**

ATLAS 采用 **Markdown 文档** 作为数据存储载体，而非传统数据库。索引服务负责：

1. **扫描识别** - 自动扫描仓库中的文档，识别其类型和数据
2. **结构化索引** - 将非结构化/半结构化文档转换为结构化索引
3. **查询服务** - 提供高效的查询、过滤、搜索能力
4. **身份认证** - 基于用户索引实现登录认证

### 核心价值

| 传统数据库 | ATLAS 索引系统 |
|-----------|----------------|
| 数据在数据库中 | 数据在 Markdown 文件中 |
| 数据库损坏 = 数据丢失 | 索引损坏 = **重建即可** |
| 数据格式固定 | 文档格式自由 |
| 迁移复杂 | 复制文件夹即迁移 |
| 需要 DBA 维护 | 自动维护 |
| 用户需要学习 | 用户只需写 Markdown |

---

## 一、索引服务架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ATLAS 索引服务架构                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         触发层 (Triggers)                            │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   ⏰ 计划任务          📝 保存触发          🔄 手动触发               │   │
│   │   - 每日全量扫描       - 文档保存时         - 管理员重建              │   │
│   │   - 每小时增量扫描     - 文档删除时         - API 调用                │   │
│   │                        - 文档移动时                                  │   │
│   │                                                                      │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │                                          │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                       索引引擎 (Index Engine)                        │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   📄 文档解析器        🏷️ 类型识别器        📊 索引生成器             │   │
│   │   - Markdown 解析      - doc-type 识别      - 提取关键字段            │   │
│   │   - Frontmatter        - atlas.function    - 建立映射关系            │   │
│   │   - 数据块解析         - 特征标记识别       - 生成索引记录            │   │
│   │                                                                      │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │                                          │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        索引存储 (.atlas/indexes/)                    │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   users.json     projects.json    clients.json    orders.json       │   │
│   │   ├─ 用户ID      ├─ 项目ID        ├─ 客户ID       ├─ 订单ID          │   │
│   │   ├─ 用户名      ├─ 项目名        ├─ 公司名       ├─ 金额            │   │
│   │   ├─ 密码哈希    ├─ 状态          ├─ 联系人       ├─ 状态            │   │
│   │   ├─ 角色       ├─ 负责人        ├─ 等级         ├─ 客户            │   │
│   │   └─ 文档路径    └─ 文档路径      └─ 文档路径     └─ 文档路径         │   │
│   │                                                                      │   │
│   │   _meta.json  ← 索引元信息                                           │   │
│   │   user-logs.json  ← 用户日志                                         │   │
│   │                                                                      │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │                                          │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         查询层 (Query Layer)                         │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                      │   │
│   │   🔐 认证服务          📋 列表查询          🔍 搜索服务               │   │
│   │   - 用户登录           - 项目列表           - 全文搜索               │   │
│   │   - 权限验证           - 客户列表           - 字段过滤               │   │
│   │                        - 订单列表           - 聚合统计               │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 文档层与索引层

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   📁 文档层（用户自由管理）          →       📊 索引层（系统自动整理）          │
│                                                                              │
│   repository/                              .atlas/indexes/                   │
│   ├── 随便放/安宁.md                       ├── users.json      ← 用户索引    │
│   ├── 某个目录/项目A.md                    ├── projects.json   ← 项目索引    │
│   ├── 归档/2025/订单001.md                 ├── orders.json     ← 订单索引    │
│   └── 乱七八糟/客户张三.md                  ├── clients.json    ← 客户索引    │
│                                            └── finances.json   ← 财务索引    │
│                                                                              │
│   用户：随意存放，怎么方便怎么来              系统：自动整理成专业数据格式        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、多级文档识别机制

### 2.1 问题场景

不同用户创建的文档格式可能完全不同：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          两种文档格式对比                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   👤 张三的文档（规范）                   👤 李四的文档（自由）                 │
│   ─────────────────────                 ─────────────────────               │
│                                                                              │
│   ---                                   # 王五                               │
│   doc-type: contact                                                         │
│   title: 赵六                            手机：13912345678                    │
│   atlas:                                 邮箱：wangwu@qq.com                 │
│     function: entity_detail                                                 │
│   _components:                           ## 个人信息                         │
│     comp_phone:                          - 性别：男                          │
│       type: phone                        - 生日：1990-05-15                  │
│       ...                                                                   │
│   ---                                    ## 备注                             │
│                                          这是一个重要客户                     │
│   # 赵六                                                                     │
│                                                                              │
│   ## 个人信息                                                                │
│                                                                              │
│   ```atlas-data                                                             │
│   type: contact_personal_info                                               │
│   data:                                                                      │
│     mobile: "13800138000"                                                   │
│   ```                                                                        │
│                                                                              │
│   ✅ 结构化数据                           ❓ 纯文本，无结构                     │
│   ✅ 明确的字段定义                       ❓ 信息分散在各处                     │
│   ✅ 索引可直接提取                       ❓ 需要智能识别                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 多级识别架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        索引系统 - 多级识别架构                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                            📄 输入文档                                       │
│                                 │                                            │
│                                 ▼                                            │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    Level 1: 规范匹配                                 │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  检查: doc-type + atlas-data 块                                      │   │
│   │  条件: frontmatter.doc-type === 'contact' &&                        │   │
│   │        存在 account_auth 数据块 &&                                   │   │
│   │        can_login === true                                           │   │
│   │  结果: 🟢 完全匹配 → 直接索引，confidence: 100%                       │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │ 不匹配                                    │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    Level 2: 语义匹配                                 │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  检查: frontmatter 中的关键字段                                       │   │
│   │  条件: frontmatter.type 包含 'user/contact/用户/联系人' ||           │   │
│   │        frontmatter.category === 'user' ||                           │   │
│   │        frontmatter.tags 包含 '用户/联系人'                            │   │
│   │  结果: 🟡 部分匹配 → 尝试提取，confidence: 70-90%                     │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │ 不匹配                                    │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    Level 3: 内容推断                                 │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  检查: 文档内容中的特征模式                                           │   │
│   │  规则:                                                               │   │
│   │    - 包含 "手机/电话" + 11位数字 → 可能是联系人                       │   │
│   │    - 包含 "邮箱/email" + xxx@xxx → 可能是联系人                       │   │
│   │    - 包含 "用户名/密码/登录" → 可能是用户                              │   │
│   │    - 文件路径包含 "联系人/用户/客户" → 提高置信度                       │   │
│   │  结果: 🟠 推断匹配 → 提取+标记待确认，confidence: 40-70%              │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │ 不匹配                                    │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    Level 4: 不匹配                                   │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │  结果: 🔴 不索引该文档到用户索引                                      │   │
│   │        但可能索引到其他索引（如 notes、articles）                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、用户身份状态模型

### 3.1 身份状态机

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        用户身份状态机                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                          📄 文档识别                                         │
│                               │                                              │
│           ┌───────────────────┼───────────────────┐                         │
│           ▼                   ▼                   ▼                         │
│   ┌───────────────┐   ┌───────────────┐   ┌───────────────┐                │
│   │  🔴 未识别     │   │  🟡 疑似用户   │   │  🟢 正式用户   │                │
│   │  (unknown)    │   │  (suspected)  │   │  (verified)   │                │
│   ├───────────────┤   ├───────────────┤   ├───────────────┤                │
│   │ 不像用户文档   │   │ 像用户但数据   │   │ 完整数据      │                │
│   │ 不进入用户索引 │   │ 不完整        │   │ 已认证        │                │
│   │               │   │               │   │               │                │
│   │ 权限: 无      │   │ 权限: 只读    │   │ 权限: 可登录  │                │
│   └───────────────┘   └───────┬───────┘   └───────────────┘                │
│                               │                   ▲                         │
│                               │                   │                         │
│                               │    ┌──────────────┴──────────────┐         │
│                               │    │                             │         │
│                               ▼    │                             │         │
│                    ┌─────────────────────────┐                   │         │
│                    │     认证转换触发         │                   │         │
│                    ├─────────────────────────┤                   │         │
│                    │ 1. 用户主动认领          │───────────────────┤         │
│                    │ 2. 管理员审核批准        │───────────────────┤         │
│                    │ 3. 邀请链接激活          │───────────────────┘         │
│                    └─────────────────────────┘                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 身份比喻

| 身份状态 | 比喻 | 特征 | 权限 | 转换方式 |
|---------|------|------|------|---------|
| 🔴 **未识别** | 路人甲 | 不像用户文档，不在名册中 | 无 | - |
| 🟡 **疑似用户** | 游击队 | 有部分用户特征，缺关键数据 | 只读，不能登录 | 认领/审核/邀请 |
| 🟢 **正式用户** | 正规军 | 完整档案，已认证 | 可登录，可执行任务 | - |

### 3.3 状态判定条件

```yaml
# 正式用户（正规军）条件
verified_user:
  required_all:
    - has_username: true
    - has_password_hash: true
    - can_login: true
  required_any:
    - has_user_id: true
    - has_email: true
  confidence: ">= 80"

# 疑似用户（游击队）条件  
suspected_user:
  required_any:
    - has_name: true
    - has_phone: true
    - has_email: true
  missing_any:
    - username
    - password_hash
  confidence: ">= 40"

# 未识别（路人）
unknown:
  confidence: "< 40"
```

---

## 四、用户特征标记系统

### 4.1 特征标记定义

用于识别"这是一个人/用户"的关键特征：

```yaml
# .atlas/config/user-detection.yaml
# 用户识别特征标记规则

user_identity_markers:
  # === 强标记（高置信度）===
  strong:
    - marker: username
      patterns: ["用户名", "账号", "账户", "登录名", "username", "account"]
      weight: 30
      
    - marker: password
      patterns: ["密码", "口令", "password", "passwd"]
      weight: 25
      
    - marker: user_id
      patterns: ["用户ID", "用户编号", "ID", "编号", "工号", "员工号"]
      weight: 25
      
    - marker: role
      patterns: ["角色", "权限", "职位", "role", "permission"]
      weight: 20

  # === 中等标记（中置信度）===
  medium:
    - marker: name
      patterns: ["姓名", "名字", "真实姓名", "name", "fullname"]
      weight: 15
      
    - marker: phone
      patterns: ["手机", "电话", "联系方式", "phone", "mobile", "tel"]
      validation: "1[3-9]\\d{9}"
      weight: 15
      
    - marker: email
      patterns: ["邮箱", "邮件", "email", "mail"]
      validation: "[\\w.-]+@[\\w.-]+"
      weight: 15
      
    - marker: id_card
      patterns: ["身份证", "证件号", "id_card"]
      validation: "\\d{17}[\\dXx]"
      weight: 15

  # === 弱标记（低置信度，辅助判断）===
  weak:
    - marker: gender
      patterns: ["性别", "gender", "sex"]
      weight: 5
      
    - marker: birthday
      patterns: ["生日", "出生日期", "birthday", "birth"]
      weight: 5
      
    - marker: address
      patterns: ["地址", "住址", "address"]
      weight: 5
      
    - marker: avatar
      patterns: ["头像", "照片", "avatar", "photo"]
      weight: 5

# 置信度阈值
thresholds:
  verified_user: 80      # ≥80 = 正式用户（需有强标记）
  suspected_user: 40     # ≥40 = 疑似用户
  unknown: 0             # <40 = 未识别

# 正式用户必须条件
verified_requirements:
  must_have_any:         # 至少有其中一个
    - username
    - user_id
  must_have_all:         # 必须全部有
    - password           # 必须有密码
```

### 4.2 内容推断规则

```yaml
# 内容推断配置
content_inference:
  # 字段提取规则
  field_patterns:
    name:
      patterns:
        - "^#\\s*(.+)$"                           # 一级标题
        - "姓名[：:]\s*(.+)"
        - "名字[：:]\s*(.+)"
      priority: [1, 2, 3]
      
    phone:
      patterns:
        - "(?:手机|电话|联系方式)[：:]?\s*(1[3-9]\\d{9})"
        - "(1[3-9]\\d{9})"
      validation: "^1[3-9]\\d{9}$"
      
    email:
      patterns:
        - "(?:邮箱|email|Email)[：:]?\s*([\\w.-]+@[\\w.-]+)"
        - "([\\w.-]+@[\\w.-]+\\.[a-z]{2,})"
      validation: "^[\\w.-]+@[\\w.-]+\\.[a-z]{2,}$"
      
    gender:
      patterns:
        - "性别[：:]\s*(男|女|保密)"
      mapping:
        男: male
        女: female
        保密: unknown

  # 路径加权
  path_boost:
    patterns:
      - path: "**/联系人/**"
        confidence_boost: 15
      - path: "**/用户/**"
        confidence_boost: 15
      - path: "**/客户/**"
        confidence_boost: 10
      - path: "**/团队/**"
        confidence_boost: 10

  # 内容特征加权
  content_boost:
    rules:
      - has_phone: 10                # 有手机号 +10
      - has_email: 10                # 有邮箱 +10
      - has_name: 5                  # 有姓名 +5
      - has_multiple_contact_fields: 15  # 有多个联系字段 +15
  
  min_confidence: 40
  max_confidence: 70
```

---

## 五、索引数据结构

### 5.1 用户索引格式

```json
{
  "_meta": {
    "index_name": "users",
    "version": "1.0",
    "last_full_scan": "2026-01-05T03:00:00Z",
    "last_update": "2026-01-05T14:32:15Z",
    "stats": {
      "total": 156,
      "verified": 45,
      "suspected": 12,
      "documents_scanned": 1200
    }
  },
  
  "records": {
    "yhABC12": {
      "_path": "/联系人/赵六.md",
      "_status": "verified",
      "_confidence": 100,
      "_verified_at": "2026-01-05T10:00:00Z",
      "_verified_by": "system",
      "_checksum": "a1b2c3d4",
      "_indexed_at": "2026-01-05T14:32:15Z",
      
      "id": "yhABC12",
      "username": "zhaoliu",
      "password_hash": "$2b$10$...",
      "role": "管理员",
      "status": "启用",
      "can_login": true,
      "name": "赵六",
      "email": "zhaoliu@example.com",
      "phone": "13800138000",
      "avatar": "/联系人/用户头像/zhaoliu.jpg"
    },
    
    "suspected_wangwu_001": {
      "_path": "/随便放的/王五.md",
      "_status": "suspected",
      "_confidence": 55,
      "_detected_markers": [
        { "marker": "name", "value": "王五", "source": "title" },
        { "marker": "phone", "value": "13912345678", "source": "content" },
        { "marker": "email", "value": "wangwu@qq.com", "source": "content" }
      ],
      "_missing_markers": ["username", "password", "user_id"],
      "_claim_token": "abc123...",
      "_indexed_at": "2026-01-05T14:35:22Z",
      
      "id": "suspected_wangwu_001",
      "username": null,
      "password_hash": null,
      "role": null,
      "status": null,
      "can_login": false,
      "name": "王五",
      "email": "wangwu@qq.com",
      "phone": "13912345678"
    }
  },
  
  "by_username": {
    "zhaoliu": "yhABC12"
  },
  
  "by_email": {
    "zhaoliu@example.com": "yhABC12",
    "wangwu@qq.com": "suspected_wangwu_001"
  },
  
  "by_phone": {
    "13800138000": "yhABC12",
    "13912345678": "suspected_wangwu_001"
  }
}
```

### 5.2 索引记录字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `_path` | string | 源文档路径 |
| `_status` | enum | `verified` / `suspected` |
| `_confidence` | number | 置信度 0-100 |
| `_verified_at` | datetime | 认证时间（正式用户） |
| `_verified_by` | string | 认证来源：`system` / `admin:xxx` / `self` |
| `_detected_markers` | array | 检测到的特征标记（疑似用户） |
| `_missing_markers` | array | 缺失的关键标记（疑似用户） |
| `_claim_token` | string | 认领令牌（疑似用户） |
| `_checksum` | string | 源文档校验和，用于增量更新 |
| `_indexed_at` | datetime | 索引时间 |

---

## 六、身份转换流程

### 6.1 方式1：用户主动认领

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     用户主动认领流程                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   场景: 王五发现系统中有自己的文档，想要获得登录权限                             │
│                                                                              │
│   Step 1: 访问认领页面                                                       │
│   ─────────────────────────────────────────────────────────────────────     │
│   URL: /claim?token=abc123...                                               │
│   或: /claim?email=wangwu@qq.com                                            │
│                                                                              │
│   Step 2: 身份验证                                                           │
│   ─────────────────────────────────────────────────────────────────────     │
│   系统发送验证码到: wangwu@qq.com 或 139****5678                              │
│                                                                              │
│   ┌──────────────────────────────────────────────────────────────────────┐  │
│   │  请输入收到的验证码: [______]                                          │  │
│   └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│   Step 3: 补全信息                                                           │
│   ─────────────────────────────────────────────────────────────────────     │
│   ┌──────────────────────────────────────────────────────────────────────┐  │
│   │  我们在系统中找到了您的信息:                                            │  │
│   │  ┌────────────────────────────────────────────────────────────────┐   │  │
│   │  │  姓名: 王五           ✅ 已识别                                  │   │  │
│   │  │  手机: 13912345678   ✅ 已验证                                  │   │  │
│   │  │  邮箱: wangwu@qq.com ✅ 已验证                                  │   │  │
│   │  └────────────────────────────────────────────────────────────────┘   │  │
│   │                                                                        │  │
│   │  请设置您的登录信息:                                                    │  │
│   │  ┌────────────────────────────────────────────────────────────────┐   │  │
│   │  │  用户名: [wangwu________] (用于登录)                             │   │  │
│   │  │  密码:   [••••••••••••__] 强度: ████████░░                      │   │  │
│   │  │  确认:   [••••••••••••__]                                       │   │  │
│   │  └────────────────────────────────────────────────────────────────┘   │  │
│   │                                                                        │  │
│   │                                         [✓ 完成认领，激活账号]          │  │
│   └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│   Step 4: 身份转换                                                           │
│   ─────────────────────────────────────────────────────────────────────     │
│   🟡 suspected_wangwu_001  ────────▶  🟢 yhWANG01                            │
│      游击队                             正规军                               │
│                                                                              │
│   • 生成正式用户ID                                                           │
│   • 密码加密存储                                                             │
│   • 更新索引状态                                                             │
│   • 原文档格式化（可选）                                                      │
│   • 记录认领日志                                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 方式2：管理员审核批准

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     管理员审核批准流程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  📋 疑似用户列表                                          管理后台   │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │                                                                      │   │
│   │  🟡 王五       wangwu@qq.com    置信度 55%    /随便放的/王五.md      │   │
│   │     检测到: 姓名、手机、邮箱                                         │   │
│   │     缺失: 用户名、密码、角色                                         │   │
│   │                                                                      │   │
│   │     [👁 查看文档] [✅ 批准并设置] [❌ 移出用户索引]                    │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   点击"批准并设置"后:                                                        │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  ✅ 批准用户: 王五                                             [×]   │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │                                                                      │   │
│   │  已识别信息:                                                         │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │  姓名: 王五           邮箱: wangwu@qq.com                      │   │   │
│   │  │  手机: 13912345678                                           │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │  设置登录信息:                                                       │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │  用户名: [wangwu________]                                     │   │   │
│   │  │  初始密码: [🎲 自动生成]  或  [手动设置...]                     │   │   │
│   │  │  角色: [客户 ▼]                                               │   │   │
│   │  │  ☑ 首次登录强制修改密码                                        │   │   │
│   │  │  ☑ 发送激活邮件通知用户                                        │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │  文档处理:                                                           │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │  ● 转换为标准格式 (推荐)                                       │   │   │
│   │  │  ○ 保持原格式                                                  │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │                               [取消]  [✅ 批准并激活]                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 方式3：邀请链接激活

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     邀请链接激活流程                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   管理员 ──▶ 点击"发送邀请" ──▶ 系统生成邀请链接                              │
│                                                                              │
│   邮件内容:                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  主题: 邀请您激活 ATLAS 系统账号                                     │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │                                                                      │   │
│   │  您好，王五：                                                        │   │
│   │                                                                      │   │
│   │  我们在系统中发现了您的信息文档，诚邀您激活账号以获得登录权限。          │   │
│   │                                                                      │   │
│   │  点击以下链接完成激活:                                                │   │
│   │  https://atlas.example.com/activate?token=xyz789...                 │   │
│   │                                                                      │   │
│   │  链接有效期: 7 天                                                    │   │
│   │                                                                      │   │
│   │  如果这不是您，请忽略此邮件。                                         │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   用户点击链接 ──▶ 设置用户名密码 ──▶ 🟢 完成激活                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 七、用户认证流程

### 7.1 基于索引的认证

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户认证流程（基于索引）                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   1. 用户文档存放（用户自由选择位置）                                           │
│   ─────────────────────────────────────────────────────────────────────     │
│   repository/                                                               │
│   ├── 联系人/安宁.md              ← 管理员用户                                │
│   ├── 客户/VIP/张三.md            ← 客户用户                                 │
│   ├── 团队/销售部/李四.md          ← 员工用户                                │
│   └── 归档/离职/王五.md           ← 已禁用用户（can_login: false）           │
│                                                                              │
│   2. 索引服务自动扫描                                                         │
│   ─────────────────────────────────────────────────────────────────────     │
│   IndexService 扫描所有 .md 文件，识别满足条件的用户文档：                       │
│   - doc-type: contact                                                       │
│   - 包含 account_auth 数据块                                                 │
│   - can_login: true                                                         │
│                                                                              │
│   3. 生成用户索引                                                             │
│   ─────────────────────────────────────────────────────────────────────     │
│   .atlas/indexes/users.json                                                 │
│   {                                                                         │
│     "records": {                                                            │
│       "yhABC12": {                                                          │
│         "_path": "/联系人/安宁.md",                                          │
│         "username": "anning",                                               │
│         "password_hash": "$2b$10$...",                                      │
│         "role": "管理员",                                                    │
│         "can_login": true                                                   │
│       }                                                                     │
│     },                                                                      │
│     "by_username": { "anning": "yhABC12" }                                  │
│   }                                                                         │
│                                                                              │
│   4. 用户登录                                                                 │
│   ─────────────────────────────────────────────────────────────────────     │
│   POST /api/auth/login                                                      │
│   { "username": "anning", "password": "myPassword123" }                     │
│                                                                              │
│   AuthService:                                                              │
│   ├── 1. 从索引查找用户: users.by_username["anning"] → "yhABC12"            │
│   ├── 2. 获取用户记录: users.records["yhABC12"]                              │
│   ├── 3. 检查状态: _status === "verified" && can_login === true             │
│   ├── 4. 验证密码: bcrypt.compare(password, password_hash)                  │
│   ├── 5. 生成 Token: JWT { userId, role, permissions }                      │
│   └── 6. 记录日志: logLogin(userId, { ip, device, time })                   │
│                                                                              │
│   5. 后续请求鉴权                                                             │
│   ─────────────────────────────────────────────────────────────────────     │
│   请求头: Authorization: Bearer <token>                                      │
│   中间件: verifyToken → 获取 userId → 从索引获取用户信息 → 检查权限            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 疑似用户登录拦截

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     疑似用户登录拦截流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   场景: 用户尝试用疑似用户的邮箱登录                                            │
│                                                                              │
│   POST /api/auth/login                                                      │
│   { "username": "wangwu@qq.com", "password": "xxx" }                        │
│                                                                              │
│   AuthService:                                                              │
│   ├── 1. 从索引查找: users.by_email["wangwu@qq.com"]                        │
│   │      → "suspected_wangwu_001"                                           │
│   ├── 2. 获取记录: users.records["suspected_wangwu_001"]                    │
│   ├── 3. 检查状态: _status === "suspected"                                  │
│   └── 4. 返回特殊响应:                                                       │
│                                                                              │
│   Response:                                                                 │
│   {                                                                         │
│     "success": false,                                                       │
│     "error": "account_not_activated",                                       │
│     "message": "您的账号尚未激活，请先完成认领流程",                            │
│     "claimUrl": "/claim?email=wangwu@qq.com",                               │
│     "detectedInfo": {                                                       │
│       "name": "王五",                                                       │
│       "email": "wangwu@qq.com",                                             │
│       "phone": "139****5678"                                                │
│     }                                                                       │
│   }                                                                         │
│                                                                              │
│   前端展示:                                                                   │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  ⚠️ 账号尚未激活                                                     │   │
│   │                                                                      │   │
│   │  我们在系统中找到了您的信息:                                          │   │
│   │  姓名: 王五                                                          │   │
│   │  邮箱: wangwu@qq.com                                                │   │
│   │                                                                      │   │
│   │  请先完成账号激活后再登录。                                           │   │
│   │                                                                      │   │
│   │  [前往激活账号]                                                       │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 八、索引更新机制

### 8.1 触发方式

```yaml
# 索引更新触发配置
triggers:
  # 计划任务
  schedules:
    full_scan:
      enabled: true
      cron: "0 3 * * *"           # 每天凌晨3点全量扫描
    incremental_scan:
      enabled: true
      interval: 3600               # 每小时增量扫描

  # 事件触发
  events:
    on_save: true                  # 保存文档时
    on_delete: true                # 删除文档时
    on_move: true                  # 移动文档时
    on_rename: true                # 重命名文档时

  # 手动触发
  manual:
    api: "POST /api/indexes/rebuild"
    admin_ui: true
```

### 8.2 更新流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          索引更新流程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   场景1: 用户保存文档                                                         │
│   ─────────────────────────────────────────────────────────────────────     │
│                                                                              │
│   用户编辑 安宁.md  ──▶  点击保存  ──▶  后端 API                              │
│                                              │                               │
│                                              ▼                               │
│                                   ┌─────────────────────┐                   │
│                                   │  1. 保存文件到磁盘   │                   │
│                                   └──────────┬──────────┘                   │
│                                              │                               │
│                                              ▼                               │
│                                   ┌─────────────────────┐                   │
│                                   │  2. 触发索引更新    │                   │
│                                   │  - 解析文档类型     │                   │
│                                   │  - 确定影响的索引   │                   │
│                                   │  - 更新索引记录     │                   │
│                                   └──────────┬──────────┘                   │
│                                              │                               │
│                                              ▼                               │
│                                   ┌─────────────────────┐                   │
│                                   │  3. 返回保存成功    │                   │
│                                   └─────────────────────┘                   │
│                                                                              │
│   场景2: 计划任务全量扫描                                                      │
│   ─────────────────────────────────────────────────────────────────────     │
│                                                                              │
│   每天凌晨 3:00  ──▶  扫描所有 .md 文件  ──▶  对比 checksum                   │
│                                                      │                       │
│                          ┌───────────────────────────┴───────────────┐      │
│                          ▼                                           ▼      │
│                   checksum 相同                              checksum 不同   │
│                   跳过，无需更新                              重新解析并更新   │
│                                                                              │
│   场景3: 索引损坏恢复                                                         │
│   ─────────────────────────────────────────────────────────────────────     │
│                                                                              │
│   管理员  ──▶  POST /api/indexes/rebuild/all  ──▶  删除所有索引              │
│                                                          │                   │
│                                                          ▼                   │
│                                                   全量扫描重建               │
│                                                          │                   │
│                                                          ▼                   │
│                                                   索引完全恢复！             │
│                                                   (因为源数据在文档中)        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 九、安全策略

### 9.1 疑似用户安全策略

```yaml
security:
  suspected_users:
    # 默认禁止登录
    default_can_login: false
    
    # 开启登录的条件
    login_requirements:
      min_confidence: 70           # 最低置信度
      min_completeness: 80         # 最低完整度
      required_fields:             # 必须字段
        - username
        - password_hash
      review_required: true        # 需要人工审核
    
    # 自动生成密码策略
    auto_generated_password:
      enabled: true
      length: 16
      require_change_on_first_login: true
      
    # 认领令牌策略
    claim_token:
      expire_hours: 168            # 7天有效期
      max_attempts: 5              # 最多尝试次数
      
    # 审计追踪
    audit:
      log_inferred_records: true
      log_completions: true
      notify_admin: true
```

### 9.2 密码安全

```yaml
password_security:
  # 哈希算法
  algorithm: bcrypt
  salt_rounds: 10
  
  # 密码强度要求
  strength:
    min_length: 8
    require_uppercase: true
    require_lowercase: true
    require_number: true
    require_special: false
  
  # 密码策略
  policy:
    max_age_days: 90               # 90天过期
    history_count: 5               # 不能使用最近5个密码
    lockout_attempts: 5            # 5次错误锁定
    lockout_duration_minutes: 30   # 锁定30分钟
```

---

## 十、API 设计

### 10.1 索引管理 API

```typescript
// === 索引管理 ===

// 获取索引状态
GET /api/indexes/status
Response: {
  indexes: {
    name: string
    recordCount: number
    verifiedCount: number
    suspectedCount: number
    lastUpdate: string
    status: 'healthy' | 'stale' | 'rebuilding'
  }[]
}

// 重建指定索引
POST /api/indexes/:indexName/rebuild

// 重建所有索引
POST /api/indexes/rebuild/all

// 查询索引数据
GET /api/indexes/:indexName
Query: {
  status?: 'verified' | 'suspected'
  filter?: Record<string, any>
  sort?: string
  order?: 'asc' | 'desc'
  page?: number
  limit?: number
}
```

### 10.2 疑似用户管理 API

```typescript
// === 疑似用户管理 ===

// 获取疑似用户列表
GET /api/users/suspected
Response: {
  users: SuspectedUser[]
  total: number
}

// 批准疑似用户
POST /api/users/suspected/:id/approve
Request: {
  username: string
  password?: string
  role: string
  convertDocument: boolean
  sendNotification: boolean
}
Response: {
  success: boolean
  newUserId: string
  generatedPassword?: string
}

// 移出用户索引
POST /api/users/suspected/:id/reject
Request: {
  reason?: string
}

// 发送邀请链接
POST /api/users/suspected/:id/invite
Request: {
  email?: string
  expireHours?: number
}
Response: {
  inviteLink: string
  expireAt: string
}
```

### 10.3 用户认领 API

```typescript
// === 用户认领 ===

// 请求验证码
POST /api/claim/request-code
Request: {
  email?: string
  phone?: string
}
Response: {
  success: boolean
  method: 'email' | 'sms'
  expireMinutes: number
}

// 验证并激活
POST /api/claim/verify
Request: {
  email?: string
  phone?: string
  code: string
  username: string
  password: string
}
Response: {
  success: boolean
  userId: string
  token: string
}

// 通过邀请链接激活
POST /api/claim/activate
Request: {
  token: string
  username: string
  password: string
}
Response: {
  success: boolean
  userId: string
  loginToken: string
}
```

---

## 十一、管理界面

### 11.1 索引管理面板

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📊 索引服务管理                                              [🔄 重建全部]  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  索引名称      总记录     正式     疑似     最后更新           状态     │   │
│  │  ─────────────────────────────────────────────────────────────────  │   │
│  │  👥 users       156       45       12      2分钟前           🟢 正常  │   │
│  │  📋 projects     89       89        0      15分钟前          🟢 正常  │   │
│  │  👔 clients     234      234        0      1小时前           🟡 陈旧  │   │
│  │  📦 orders      567      567        0      2小时前           🟡 陈旧  │   │
│  │  💰 finances    123      123        0      重建中...         🔵 重建  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  下次全量扫描: 2026-01-06 03:00:00                                           │
│  下次增量扫描: 2026-01-05 16:00:00                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 11.2 用户索引管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  👥 用户索引管理                                              [🔄 重建索引]  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📈 统计概览                                                         │   │
│  │  ────────────────────────────────────────────────────────────────── │   │
│  │  总用户: 156    🟢 正式: 45    🟡 疑似: 12    扫描文档: 1,200         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  筛选: [全部 ▼]  [🟢 正式用户] [🟡 疑似用户] [🔴 不可登录]            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  用户列表                                                            │   │
│  │  ────────────────────────────────────────────────────────────────── │   │
│  │                                                                      │   │
│  │  🟢 赵六        zhaoliu       管理员    100%   /联系人/赵六.md        │   │
│  │                                                                      │   │
│  │  🟢 张三        zhangsan      客户      95%    /客户/VIP/张三.md      │   │
│  │                                                                      │   │
│  │  🟡 王五        -             -         55%    /随便放的/王五.md      │   │
│  │     ⚠️ 疑似用户 - 缺少: 用户名、密码、角色                            │   │
│  │     [📝 补全信息] [✅ 批准] [📧 发送邀请] [❌ 移出]                    │   │
│  │                                                                      │   │
│  │  🟡 临时用户    -             -         42%    /草稿/某人.md          │   │
│  │     ⚠️ 疑似用户 - 缺少: 用户名、密码、角色、邮箱                       │   │
│  │     [📝 补全信息] [✅ 批准] [📧 发送邀请] [❌ 移出]                    │   │
│  │                                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 十二、开发计划

### 12.1 任务清单

| 阶段 | 任务 | 优先级 | 预估工期 | 依赖项 |
|------|------|--------|----------|--------|
| **4.2.1** | 索引服务核心架构 | 🔴 高 | 1 周 | - |
| **4.2.2** | 文档解析器（多级识别） | 🔴 高 | 1 周 | 4.2.1 |
| **4.2.3** | 用户特征标记系统 | 🔴 高 | 3 天 | 4.2.2 |
| **4.2.4** | 用户索引生成 | 🔴 高 | 3 天 | 4.2.3 |
| **4.2.5** | 索引触发机制（事件/计划） | 🟡 中 | 3 天 | 4.2.4 |
| **4.2.6** | 用户认证服务 | 🔴 高 | 3 天 | 4.2.4 |
| **4.2.7** | 疑似用户管理 API | 🟡 中 | 2 天 | 4.2.4 |
| **4.2.8** | 用户认领流程 | 🟡 中 | 3 天 | 4.2.7 |
| **4.2.9** | 管理员审核功能 | 🟡 中 | 2 天 | 4.2.7 |
| **4.2.10** | 邀请链接功能 | 🟢 低 | 2 天 | 4.2.7 |
| **4.2.11** | 管理界面开发 | 🟡 中 | 1 周 | 4.2.7 |
| **4.2.12** | 其他索引类型（项目/客户/订单） | 🟢 低 | 1 周 | 4.2.4 |

### 12.2 里程碑

```
Week 1-2:  索引服务核心 + 文档解析器 + 特征标记系统
Week 3:    用户索引生成 + 触发机制 + 认证服务
Week 4:    疑似用户管理 + 认领流程 + 审核功能
Week 5:    邀请链接 + 管理界面
Week 6:    其他索引类型 + 测试优化
```

---

## 附录

### A. 索引类型清单

| 索引名称 | 匹配条件 | 核心字段 | 用途 |
|----------|----------|----------|------|
| `users` | doc-type: contact + can_login | id, username, password_hash, role | 用户认证 |
| `projects` | doc-type: project | id, name, status, owner | 项目管理 |
| `clients` | doc-type: client | id, name, company, level | 客户管理 |
| `orders` | doc-type: order | id, client_id, amount, status | 订单管理 |
| `products` | doc-type: product | id, name, price, stock | 产品管理 |
| `tasks` | doc-type: task | id, title, status, assignee | 任务管理 |
| `invoices` | doc-type: invoice | id, client_id, amount, status | 财务管理 |

### B. 用户状态对照表

| 状态 | 英文 | 比喻 | 置信度 | 可登录 | 转换方式 |
|------|------|------|--------|--------|---------|
| 🔴 未识别 | `unknown` | 路人甲 | <40 | ❌ | - |
| 🟡 疑似 | `suspected` | 游击队 | 40-79 | ❌ | 认领/审核/邀请 |
| 🟢 正式 | `verified` | 正规军 | ≥80 | ✅ | - |

### C. 相关文档

- [Phase-4.1-类型插件包与数据块资产池](./Phase-4.1-类型插件包与数据块资产池.md)
- [数据块组件插件化开发标准与注册机制](./数据块组件插件化开发标准与注册机制.md)

---

*文档版本: 1.0.0*  
*创建日期: 2026-01-05*  
*作者: ATLAS Team*

