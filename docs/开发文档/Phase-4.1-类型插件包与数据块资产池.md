# Phase 4.1 - 类型插件包与数据块资产池

## 背景

在 Phase 4.0 完成文档属性体系重构后，我们识别出一个核心架构问题：

### 当前问题

1. **四大核心概念各自独立**：文档类型、功能类型、显现模式、能力配置互不关联，用户需要分别配置
2. **配置组合可能无意义**：用户类型文档配置思维导图视图、订单文档配置文章模式，逻辑上不合理
3. **新建文档成本高**：用户需要理解所有配置项，手动填写 frontmatter
4. **数据块使用分散**：没有统一的数据块管理机制，无法复用和共享

### 核心洞察

> **文档类型是第一性原理，其他三个维度都应该由类型推导出来。**

```
                    ┌─────────────────┐
                    │   文档类型       │ ← 用户唯一需要选择的
                    │  (document_type) │
                    └────────┬────────┘
                             │
           ┌─────────────────┼─────────────────┐
           ▼                 ▼                 ▼
    ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
    │  功能类型    │   │  显现模式    │   │  能力配置    │
    │  (function)  │   │  (display)   │   │(capabilities)│
    └─────────────┘   └─────────────┘   └─────────────┘
           │                 │                 │
           └─────────────────┼─────────────────┘
                             ▼
                    ┌─────────────────┐
                    │  预置数据块      │ ← 该类型的标准结构
                    │  (blocks)       │
                    └─────────────────┘
```

---

## 设计目标

### 用户体验目标

| 用户类型 | 当前体验 | 目标体验 |
|----------|----------|----------|
| **新手用户** | 需要理解 5+ 个配置项，手动填写 frontmatter | 选择类型 → 填名称 → 开始编辑，零配置 |
| **普通用户** | 每个文档都要重复配置 | 一键套用类型模板，直接填写数据 |
| **进阶用户** | 无法复用自己的配置 | 可保存、分享自己的数据块和类型包 |
| **开发者** | 无标准化扩展方式 | 可开发发布类型包插件，生态共建 |

### 技术架构目标

1. **类型包插件化**：每种文档类型封装为独立插件包
2. **数据块资产化**：数据块作为可流通的数字资产
3. **配置自动化**：选择类型后自动配置所有属性
4. **生态开放化**：支持用户创建、分享、下载类型包和数据块

---

## 一、类型插件包系统

### 1.1 类型包定义

类型插件包（Type Package）是以文档类型为核心的配置单元，包含该类型文档的完整蓝图。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           类型包结构                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   📦 {package-id}/                      # 类型包根目录                        │
│   │                                                                          │
│   ├── manifest.yaml                     # 📋 包声明（必需）                   │
│   │   ├── id: "client"                  #    唯一标识                        │
│   │   ├── name: "客户管理"               #    显示名称                        │
│   │   ├── version: "1.0.0"              #    语义化版本                       │
│   │   ├── description: "..."            #    描述                            │
│   │   ├── author: "atlas_official"      #    作者                            │
│   │   ├── category: "business"          #    分类                            │
│   │   ├── icon: "users"                 #    图标                            │
│   │   └── tags: [CRM, 客户, 销售]       #    标签                            │
│   │                                                                          │
│   ├── type.yaml                         # 🏷️ 类型定义（必需）                 │
│   │   └── document_type: "client"       #    文档类型标识                     │
│   │                                                                          │
│   ├── functions.yaml                    # ⚙️ 功能配置（必需）                 │
│   │   ├── default: "entity_list"        #    默认功能                        │
│   │   └── available: [...]              #    可用功能列表                     │
│   │                                                                          │
│   ├── displays.yaml                     # 👁️ 视图配置（必需）                 │
│   │   ├── default: "list.table"         #    默认视图                        │
│   │   └── available: [...]              #    可用视图列表                     │
│   │                                                                          │
│   ├── capabilities.yaml                 # 🔧 能力配置（必需）                 │
│   │   ├── default: [crud, export]       #    默认启用能力                     │
│   │   └── available: [...]              #    可用能力列表                     │
│   │                                                                          │
│   ├── blocks/                           # 📦 预置数据块（可选）               │
│   │   ├── basic_info.yaml               #    基本信息块                       │
│   │   ├── contact.yaml                  #    联系方式块                       │
│   │   └── ...                           #    更多数据块                       │
│   │                                                                          │
│   ├── template.md                       # 📄 文档模板（必需）                 │
│   │                                      #    预置数据块的组合                 │
│   │                                                                          │
│   ├── schema.yaml                       # 📐 数据校验规则（可选）             │
│   │                                                                          │
│   ├── workflows/                        # 🔄 工作流定义（可选）               │
│   │   └── status_flow.yaml              #    状态机                          │
│   │                                                                          │
│   └── assets/                           # 🎨 静态资源（可选）                 │
│       ├── icon.svg                      #    类型图标                        │
│       └── preview.png                   #    预览图                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 manifest.yaml 完整规范

```yaml
# 类型包声明文件
# 版本: 1.0

# === 基础信息 ===
id: "client"                              # 唯一标识，全局唯一
name: "客户管理"                           # 显示名称
version: "1.0.0"                          # 语义化版本 (SemVer)
description: "标准客户关系管理类型包，包含客户基本信息、联系方式、跟进记录等数据块"
author: "atlas_official"                  # 作者 ID
license: "MIT"                            # 许可证

# === 分类与标签 ===
category: "business"                      # 主分类: system | business | content | custom
subcategory: "crm"                        # 子分类
tags:
  - CRM
  - 客户管理
  - 销售
  - B2B

# === 显示配置 ===
icon: "users"                             # Lucide 图标名
color: "#3B82F6"                          # 主题色
preview_image: "assets/preview.png"       # 预览图路径

# === 依赖与兼容 ===
atlas_version: ">=4.1.0"                  # ATLAS 版本要求
dependencies: []                          # 依赖的其他类型包
conflicts: []                             # 冲突的类型包

# === 发布信息 ===
published: true                           # 是否公开
created_at: "2026-01-04"
updated_at: "2026-01-04"
downloads: 0                              # 下载次数（系统维护）
rating: 0                                 # 评分（系统维护）
```

### 1.3 functions.yaml 功能配置

```yaml
# 功能类型配置
# 定义该文档类型可以承载的功能

default: "entity_list"                    # 新建文档时的默认功能

available:
  - id: "entity_list"
    name: "实体列表"
    description: "展示和管理实体数据列表"
    
  - id: "entity_detail"
    name: "实体详情"
    description: "单个实体的详细信息页"
    
  - id: "directory"
    name: "目录索引"
    description: "作为其他文档的导航入口"

# 功能约束
constraints:
  # 某些功能互斥
  mutually_exclusive:
    - ["entity_list", "entity_detail"]
```

### 1.4 displays.yaml 视图配置

```yaml
# 显现模式配置
# 定义该文档类型可用的视图方式

default: "list.table"                     # 新建文档时的默认视图

available:
  # 列表视图组
  - id: "list.table"
    name: "表格列表"
    icon: "table"
    description: "经典表格形式展示数据"
    
  - id: "list.card"
    name: "卡片列表"
    icon: "layout-grid"
    description: "卡片网格形式展示数据"
    
  - id: "list.compact"
    name: "紧凑列表"
    icon: "list"
    description: "紧凑行形式展示数据"

  # 详情视图组
  - id: "detail.card"
    name: "卡片详情"
    icon: "credit-card"
    
  - id: "detail.form"
    name: "表单详情"
    icon: "file-text"

  # 看板视图组
  - id: "kanban.column"
    name: "列看板"
    icon: "columns"
    requires_field: "status"              # 需要有 status 字段

# 视图约束
constraints:
  # 某些视图需要特定字段
  field_requirements:
    "kanban.*": ["status"]
    "timeline.*": ["date", "start_date"]
    "calendar.*": ["date", "start_date"]
```

### 1.5 capabilities.yaml 能力配置

```yaml
# 能力配置
# 定义该文档类型启用的能力

default:
  - crud                                  # 增删改查
  - export                                # 导出
  - import                                # 导入

available:
  # 数据操作类
  - id: "crud"
    name: "增删改查"
    description: "创建、读取、更新、删除数据"
    requires_permission: "write"
    
  - id: "export"
    name: "导出"
    description: "导出为 CSV/Excel/PDF"
    requires_permission: "read"
    
  - id: "import"
    name: "导入"
    description: "从外部文件导入数据"
    requires_permission: "write"

  # 协作类
  - id: "share"
    name: "分享"
    description: "生成分享链接"
    
  - id: "comment"
    name: "评论"
    description: "添加评论讨论"

  # 扩展类
  - id: "webhook"
    name: "Webhook"
    description: "接收外部系统通知"
    requires_permission: "admin"
    
  - id: "api"
    name: "API 访问"
    description: "通过 API 访问数据"
    requires_permission: "api"

# 能力约束
constraints:
  # 某些能力需要其他能力
  dependencies:
    "import": ["crud"]
    "webhook": ["api"]
```

### 1.6 template.md 文档模板

```markdown
---
# ====== 以下属性由类型包自动填充 ======
document_type: {{type_id}}
title: "{{title}}"
created_at: "{{created_at}}"
updated_at: "{{updated_at}}"
author: "{{author}}"

atlas:
  function: {{default_function}}
  display: {{default_displays}}
  capabilities: {{default_capabilities}}
---

# {{title}}

{{#each blocks}}
## {{this.title}}

```atlas-data
{{this.content}}
```

{{/each}}
```

### 1.7 预置数据块示例

```yaml
# blocks/basic_info.yaml
# 客户基本信息数据块

id: "client_basic_info"
name: "基本信息"
description: "客户的基本信息，包含名称、等级、来源等"
required: true                            # 是否必选

template: |
  type: basic_info
  schema:
    - key: name
      label: 客户名称
      type: text
      required: true
    - key: level
      label: 客户等级
      type: select
      options:
        - value: vip
          label: VIP
          color: "#EF4444"
        - value: important
          label: 重要
          color: "#F59E0B"
        - value: normal
          label: 普通
          color: "#3B82F6"
        - value: potential
          label: 潜在
          color: "#6B7280"
    - key: source
      label: 客户来源
      type: select
      options: [官网, 推荐, 广告, 展会, 其他]
    - key: created_date
      label: 建档日期
      type: date
  data:
    name: ""
    level: "normal"
    source: ""
    created_date: "{{today}}"
```

---

## 二、数据块资产池系统

### 2.1 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          数据块资产池生态系统                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 3: 全球资产池 (Global Asset Pool)                             │   │
│   │  ─────────────────────────────────────────────────────────────────   │   │
│   │  🌍 ATLAS 官方服务器托管                                             │   │
│   │  📡 REST API / GraphQL 接口                                         │   │
│   │  🏪 支持搜索、评分、下载统计                                          │   │
│   │  💰 未来可能支持付费资产、认证作者                                    │   │
│   │  🔒 审核机制：官方审核 / 社区投票                                     │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │ 同步/发布                                 │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 2: 系统资产池 (System Asset Pool)                             │   │
│   │  ─────────────────────────────────────────────────────────────────   │   │
│   │  🏢 当前 ATLAS 实例内的公共池                                         │   │
│   │  👥 系统内所有用户可见、可用                                          │   │
│   │  📤 用户可一键发布个人资产到此池                                      │   │
│   │  🛡️ 管理员可管理、审核、删除                                         │   │
│   └──────────────────────────────┬──────────────────────────────────────┘   │
│                                  │ 发布/收藏                                 │
│                                  ▼                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 1: 个人资产池 (Personal Asset Pool)                           │   │
│   │  ─────────────────────────────────────────────────────────────────   │   │
│   │  👤 用户私有空间                                                     │   │
│   │  ✏️ 可自由创建、编辑、删除                                           │   │
│   │  📥 从系统池/全球池收藏而来                                          │   │
│   │  🔧 可基于收藏的资产进行改造                                         │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 数据块资产结构

```yaml
# 数据块资产完整规范
# 版本: 1.0

# === 基础信息 ===
id: "contact_info_v1"                     # 全局唯一 ID
name: "联系人信息"                         # 显示名称
version: "1.0.0"                          # 版本号
description: "包含姓名、电话、邮箱、微信的标准联系人信息块"
author: "atlas_official"                  # 作者 ID

# === 资产元数据 ===
metadata:
  created_at: "2026-01-04"
  updated_at: "2026-01-04"
  downloads: 1234                         # 下载次数
  rating: 4.8                             # 评分 (1-5)
  reviews_count: 56                       # 评价数
  
# === 分类与标签 ===
category: "basic"                         # 分类: basic | business | content | custom
tags:
  - 联系人
  - 基础信息
  - CRM

# === 类型兼容性声明 ===
compatibility:
  # 模式1: 白名单模式 - 明确列出兼容的类型
  mode: "whitelist"
  types:
    - client                              # 客户
    - supplier                            # 供应商
    - employee                            # 员工
    - contact                             # 联系人
    - partner                             # 合作伙伴
    
  # 模式2: 黑名单模式 - 列出不兼容的类型
  # mode: "blacklist"
  # exclude_types:
  #   - article
  #   - note
  #   - media
  
  # 模式3: 通用模式 - 适用于所有类型
  # mode: "universal"

# === 数据块代码模板 ===
template: |
  type: contact_info
  schema:
    - key: name
      label: 姓名
      type: text
      required: true
    - key: phone
      label: 电话
      type: phone
    - key: email
      label: 邮箱
      type: email
    - key: wechat
      label: 微信
      type: text
  data:
    name: ""
    phone: ""
    email: ""
    wechat: ""

# === 预览配置 ===
preview:
  image: "/assets/blocks/contact_info_preview.png"
  demo_data:
    name: "张三"
    phone: "13800138000"
    email: "zhangsan@example.com"
    wechat: "zhangsan_wx"
```

### 2.3 兼容性声明规范

```yaml
# 兼容性声明的三种模式

# === 模式1: 白名单 (whitelist) ===
# 只有列出的类型可以使用此数据块
compatibility:
  mode: "whitelist"
  types:
    - client
    - supplier
    - employee
  
# 适用场景: 业务专属数据块，如"客户画像"只适用于客户类型

# === 模式2: 黑名单 (blacklist) ===
# 除了列出的类型外，其他都可以使用
compatibility:
  mode: "blacklist"
  exclude_types:
    - article
    - note
    - media
    
# 适用场景: 通用性较强，但某些类型明显不适用

# === 模式3: 通用 (universal) ===
# 适用于所有文档类型
compatibility:
  mode: "universal"
  
# 适用场景: 系统级数据块，如"文件附件"、"时间戳"、"标签"
```

### 2.4 资产池存储结构

```
.atlas/
├── assets/                               # 资产池根目录
│   │
│   ├── personal/                         # 个人资产池
│   │   ├── {user-id}/
│   │   │   ├── blocks/                   # 用户的数据块
│   │   │   │   ├── my_contact.yaml
│   │   │   │   └── custom_tag.yaml
│   │   │   ├── packages/                 # 用户的类型包
│   │   │   │   └── my_project_type/
│   │   │   └── favorites.yaml            # 收藏列表
│   │   └── ...
│   │
│   ├── system/                           # 系统资产池
│   │   ├── blocks/                       # 系统级数据块
│   │   │   ├── contact_info.yaml
│   │   │   ├── company_info.yaml
│   │   │   ├── follow_up.yaml
│   │   │   └── ...
│   │   ├── packages/                     # 系统级类型包
│   │   │   ├── client/
│   │   │   ├── product/
│   │   │   ├── order/
│   │   │   └── ...
│   │   └── index.yaml                    # 资产索引
│   │
│   └── cache/                            # 全球资产池缓存
│       ├── blocks/
│       │   └── ...
│       ├── packages/
│       │   └── ...
│       └── index.yaml                    # 缓存索引
│
├── packages/                             # 已安装的类型包
│   ├── client/
│   ├── product/
│   └── ...
│
└── config/
    └── asset-pool.yaml                   # 资产池配置
```

### 2.5 资产池配置文件

```yaml
# .atlas/config/asset-pool.yaml
# 资产池全局配置

# === 全球资产池连接 ===
global_pool:
  enabled: true
  endpoint: "https://assets.atlas-docs.com/api/v1"
  sync_interval: "24h"                    # 同步间隔
  auto_sync: true                         # 自动同步
  
# === 系统资产池配置 ===
system_pool:
  enabled: true
  allow_user_publish: true                # 允许用户发布到系统池
  require_approval: false                 # 是否需要管理员审批
  
# === 个人资产池配置 ===
personal_pool:
  enabled: true
  max_blocks_per_user: 100                # 每用户最大数据块数
  max_packages_per_user: 20               # 每用户最大类型包数
  
# === 缓存配置 ===
cache:
  max_size: "100MB"
  ttl: "7d"                               # 缓存有效期
```

---

## 三、用户交互流程

### 3.1 新建文档流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             新建文档流程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Step 1: 选择文档类型                                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  🆕 新建文档                                              [×]        │   │
│   │                                                                      │   │
│   │  选择文档类型:                                                        │   │
│   │                                                                      │   │
│   │  📂 业务文档                                                         │   │
│   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│   │  │ 👤      │ │ 📦      │ │ 📋      │ │ 🏭      │ │ 📊      │       │   │
│   │  │ 客户    │ │ 产品    │ │ 订单    │ │ 供应商   │ │ 项目    │       │   │
│   │  │ 管理    │ │ 管理    │ │ 管理    │ │ 管理    │ │ 管理    │       │   │
│   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│   │                                                                      │   │
│   │  📂 内容文档                                                         │   │
│   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                   │   │
│   │  │ 📰      │ │ 📝      │ │ 📚      │ │ 🎬      │                   │   │
│   │  │ 文章    │ │ 笔记    │ │ 文档    │ │ 媒体    │                   │   │
│   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘                   │   │
│   │                                                                      │   │
│   │  📂 系统文档                                                         │   │
│   │  ┌─────────┐ ┌─────────┐                                           │   │
│   │  │ 👥      │ │ 🔐      │                                           │   │
│   │  │ 用户    │ │ 权限    │                                           │   │
│   │  └─────────┘ └─────────┘                                           │   │
│   │                                                                      │   │
│   │  ➕ 浏览更多类型包...                                                 │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Step 2: 配置文档（可选展开）                                         │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  📄 新建客户文档                                          [×]        │   │
│   │                                                                      │   │
│   │  文档名称: [____________________________]                            │   │
│   │                                                                      │   │
│   │  ▼ 高级选项 (可选)                                                   │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │ 默认视图: [表格列表 ▼]                                        │   │   │
│   │  │ 保存位置: [/repository/clients/ ▼]                            │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │  📦 预置数据块:                                                      │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │ ☑ 基本信息    必选 - 客户名称、等级、来源等                    │   │   │
│   │  │ ☑ 联系方式    联系人、电话、邮箱、微信、地址                    │   │   │
│   │  │ ☑ 公司信息    公司名称、行业、规模、官网                        │   │   │
│   │  │ ☐ 跟进记录    日期、方式、内容、下一步                          │   │   │
│   │  │ ☐ 合同信息    合同编号、金额、期限                              │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │  💡 提示: 可在创建后随时添加更多数据块                               │   │
│   │                                                                      │   │
│   │                                    [取消]  [✓ 创建文档]              │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Step 3: 自动生成并跳转编辑                                          │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│   系统自动执行:                                                              │
│   1. 创建 Markdown 文件到指定位置                                           │
│   2. 填充 frontmatter (类型、功能、视图、能力 - 全部自动配置)                │
│   3. 插入勾选的预置数据块                                                    │
│   4. 跳转到编辑页面，用户直接开始填写数据                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 编辑时添加数据块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          编辑时添加数据块                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  📄 XXX公司.md                                       [阅读] [编辑]   │   │
│   │                                                                      │   │
│   │  ┌────────────────────────────────────────────────────────────┐     │   │
│   │  │  # XXX公司                                                  │     │   │
│   │  │                                                             │     │   │
│   │  │  ## 基本信息                                                │     │   │
│   │  │  ┌──────────────────────────────────────────────────────┐  │     │   │
│   │  │  │ 客户名称: XXX科技有限公司                              │  │     │   │
│   │  │  │ 客户等级: VIP                                          │  │     │   │
│   │  │  └──────────────────────────────────────────────────────┘  │     │   │
│   │  │                                                             │     │   │
│   │  │  ## 联系方式                                                │     │   │
│   │  │  ┌──────────────────────────────────────────────────────┐  │     │   │
│   │  │  │ 联系人: 张三    电话: 13800138000                      │  │     │   │
│   │  │  └──────────────────────────────────────────────────────┘  │     │   │
│   │  │                                                             │     │   │
│   │  │                 ┌──────────────────────┐                    │     │   │
│   │  │                 │  ➕ 添加数据块        │ ← 点击打开资产池   │     │   │
│   │  │                 └──────────────────────┘                    │     │   │
│   │  │                                                             │     │   │
│   │  └────────────────────────────────────────────────────────────┘     │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│                                    ↓ 点击后打开资产池面板                     │
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        📦 数据块资产池                       [×]     │   │
│   │                                                                      │   │
│   │  🔍 搜索数据块...              筛选: [我的] [系统] [全球]            │   │
│   │                                                                      │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │ ⚠️ 当前文档类型: client                                       │   │   │
│   │  │    已自动过滤，只显示兼容的数据块                              │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │  📂 推荐                                                            │   │
│   │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐       │   │
│   │  │ 📊 客户画像│ │ 📝 跟进记录│ │ 📋 合同摘要│ │ ⭐ 满意度  │       │   │
│   │  │ ⭐4.8 1.2k│ │ ⭐4.9 2.1k│ │ ⭐4.6 856 │ │ ⭐4.5 432 │       │   │
│   │  │ [预览][导入]│ │ [预览][导入]│ │ [预览][导入]│ │ [预览][导入]│       │   │
│   │  └────────────┘ └────────────┘ └────────────┘ └────────────┘       │   │
│   │                                                                      │   │
│   │  📂 我的资产                                                        │   │
│   │  ┌────────────┐ ┌────────────┐                                     │   │
│   │  │ 🔧 自定义  │ │ 📋 内部    │                                     │   │
│   │  │ 客户标签   │ │ 审批流程   │                                     │   │
│   │  │ [预览][导入]│ │ [预览][导入]│                                     │   │
│   │  └────────────┘ └────────────┘                                     │   │
│   │                                                                      │   │
│   │  ─────────────────────────────────────────────────────────────────  │   │
│   │                                                                      │   │
│   │  ➕ 创建新数据块                    📤 发布数据块到系统池            │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 数据块发布流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          数据块发布流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   用户创建数据块                                                              │
│        │                                                                     │
│        ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  📝 编辑数据块                                             [×]       │   │
│   │                                                                      │   │
│   │  名称: [自定义客户标签________________]                              │   │
│   │  描述: [用于标记客户的自定义分类标签____]                            │   │
│   │                                                                      │   │
│   │  分类: [业务 ▼]       标签: [客户][标签][分类]                       │   │
│   │                                                                      │   │
│   │  适用类型:                                                           │   │
│   │  ☑ client  ☑ supplier  ☐ product  ☐ order  ☐ article               │   │
│   │                                                                      │   │
│   │  数据块代码:                                                         │   │
│   │  ┌──────────────────────────────────────────────────────────────┐   │   │
│   │  │ type: custom_tags                                             │   │   │
│   │  │ schema:                                                       │   │   │
│   │  │   - key: tags                                                 │   │   │
│   │  │     label: 标签                                               │   │   │
│   │  │     type: tags                                                │   │   │
│   │  │     options: [重点跟进, 待开发, 长期合作, 风险客户]            │   │   │
│   │  │ data:                                                         │   │   │
│   │  │   tags: []                                                    │   │   │
│   │  └──────────────────────────────────────────────────────────────┘   │   │
│   │                                                                      │   │
│   │                           [保存到个人池]  [📤 发布到系统池]          │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│        │                              │                                      │
│        ▼                              ▼                                      │
│   ┌─────────────┐              ┌─────────────┐                              │
│   │  个人资产池  │              │  系统资产池  │ ← 系统内其他用户可见        │
│   └─────────────┘              └──────┬──────┘                              │
│                                       │                                      │
│                                       ▼ (可选) 申请发布到全球池              │
│                                ┌─────────────┐                              │
│                                │  全球资产池  │ ← 需要审核                   │
│                                └─────────────┘                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、技术实现方案

### 4.1 目录结构

```
frontend/
├── src/
│   ├── components/
│   │   ├── type-package/                 # 类型包相关组件
│   │   │   ├── PackageBrowser.tsx        # 类型包浏览器
│   │   │   ├── PackageCard.tsx           # 类型包卡片
│   │   │   ├── PackageInstaller.tsx      # 类型包安装器
│   │   │   └── NewDocumentDialog.tsx     # 新建文档对话框
│   │   │
│   │   ├── asset-pool/                   # 资产池相关组件
│   │   │   ├── AssetPoolPanel.tsx        # 资产池面板
│   │   │   ├── BlockCard.tsx             # 数据块卡片
│   │   │   ├── BlockPreview.tsx          # 数据块预览
│   │   │   ├── BlockEditor.tsx           # 数据块编辑器
│   │   │   └── BlockPublisher.tsx        # 数据块发布器
│   │   │
│   │   └── ...
│   │
│   ├── services/
│   │   ├── type-package-service.ts       # 类型包服务
│   │   ├── asset-pool-service.ts         # 资产池服务
│   │   └── ...
│   │
│   └── types/
│       ├── type-package.ts               # 类型包类型定义
│       ├── asset-pool.ts                 # 资产池类型定义
│       └── ...

backend/
├── src/
│   ├── services/
│   │   ├── type-package/
│   │   │   ├── package-manager.ts        # 类型包管理
│   │   │   ├── package-loader.ts         # 类型包加载
│   │   │   ├── package-validator.ts      # 类型包校验
│   │   │   └── template-generator.ts     # 模板生成器
│   │   │
│   │   ├── asset-pool/
│   │   │   ├── pool-manager.ts           # 资产池管理
│   │   │   ├── block-importer.ts         # 数据块导入
│   │   │   ├── block-exporter.ts         # 数据块导出
│   │   │   └── sync-service.ts           # 全球池同步
│   │   │
│   │   └── ...
│   │
│   └── routes/
│       ├── type-package.ts               # 类型包 API
│       ├── asset-pool.ts                 # 资产池 API
│       └── ...
```

### 4.2 核心 API 设计

```typescript
// === 类型包 API ===

// 获取所有可用类型包
GET /api/type-packages
Response: {
  packages: TypePackage[]
  categories: Category[]
}

// 获取单个类型包详情
GET /api/type-packages/:id
Response: TypePackage

// 基于类型包创建文档
POST /api/documents/create-from-package
Request: {
  packageId: string
  title: string
  path: string
  selectedBlocks: string[]   // 选中的预置数据块 ID
  options?: {
    defaultView?: string
    customCapabilities?: string[]
  }
}
Response: {
  success: boolean
  document: {
    path: string
    frontmatter: object
  }
}

// === 资产池 API ===

// 获取数据块列表（带过滤）
GET /api/asset-pool/blocks
Query: {
  pool: 'personal' | 'system' | 'global'
  documentType?: string      // 按文档类型过滤
  category?: string
  search?: string
  page?: number
  limit?: number
}
Response: {
  blocks: DataBlock[]
  total: number
  hasMore: boolean
}

// 导入数据块到文档
POST /api/asset-pool/blocks/import
Request: {
  blockId: string
  documentPath: string
  position?: number          // 插入位置
}
Response: {
  success: boolean
  insertedContent: string
}

// 创建/保存数据块
POST /api/asset-pool/blocks
Request: {
  pool: 'personal' | 'system'
  block: DataBlockDefinition
}
Response: {
  success: boolean
  block: DataBlock
}

// 发布数据块到更高层级池
POST /api/asset-pool/blocks/:id/publish
Request: {
  targetPool: 'system' | 'global'
}
Response: {
  success: boolean
  status: 'published' | 'pending_review'
}

// 收藏数据块
POST /api/asset-pool/blocks/:id/favorite
Response: {
  success: boolean
}

// 同步全球资产池
POST /api/asset-pool/sync
Response: {
  success: boolean
  synced: number
  errors: string[]
}
```

### 4.3 类型定义

```typescript
// types/type-package.ts

export interface TypePackageManifest {
  id: string
  name: string
  version: string
  description: string
  author: string
  license?: string
  category: 'system' | 'business' | 'content' | 'custom'
  subcategory?: string
  tags: string[]
  icon: string
  color?: string
  previewImage?: string
  atlasVersion?: string
  dependencies?: string[]
  conflicts?: string[]
  published: boolean
  createdAt: string
  updatedAt: string
  downloads: number
  rating: number
}

export interface TypePackageFunctions {
  default: string
  available: FunctionDefinition[]
  constraints?: {
    mutuallyExclusive?: string[][]
  }
}

export interface TypePackageDisplays {
  default: string
  available: DisplayDefinition[]
  constraints?: {
    fieldRequirements?: Record<string, string[]>
  }
}

export interface TypePackageCapabilities {
  default: string[]
  available: CapabilityDefinition[]
  constraints?: {
    dependencies?: Record<string, string[]>
  }
}

export interface TypePackageBlock {
  id: string
  name: string
  description: string
  required: boolean
  template: string
}

export interface TypePackage {
  manifest: TypePackageManifest
  type: { documentType: string }
  functions: TypePackageFunctions
  displays: TypePackageDisplays
  capabilities: TypePackageCapabilities
  blocks: TypePackageBlock[]
  template: string
  schema?: object
  workflows?: object
}

// types/asset-pool.ts

export interface DataBlockCompatibility {
  mode: 'whitelist' | 'blacklist' | 'universal'
  types?: string[]
  excludeTypes?: string[]
}

export interface DataBlockMetadata {
  createdAt: string
  updatedAt: string
  downloads: number
  rating: number
  reviewsCount: number
}

export interface DataBlock {
  id: string
  name: string
  version: string
  description: string
  author: string
  category: string
  tags: string[]
  compatibility: DataBlockCompatibility
  template: string
  metadata: DataBlockMetadata
  preview?: {
    image?: string
    demoData?: object
  }
}

export interface AssetPool {
  personal: {
    blocks: DataBlock[]
    packages: TypePackage[]
    favorites: string[]
  }
  system: {
    blocks: DataBlock[]
    packages: TypePackage[]
  }
  global: {
    blocks: DataBlock[]
    packages: TypePackage[]
    lastSynced: string
  }
}
```

---

## 五、开发计划

### Phase 4.1.1 - 类型包核心系统（预计 2 周）

**目标**：实现类型包的基础架构和内置类型包

#### 任务清单

- [ ] **4.1.1.1 类型包数据结构**
  - [ ] 定义 TypeScript 类型定义
  - [ ] 实现 manifest.yaml 解析器
  - [ ] 实现类型包校验器
  - [ ] 编写单元测试

- [ ] **4.1.1.2 类型包加载器**
  - [ ] 实现文件系统类型包扫描
  - [ ] 实现类型包索引构建
  - [ ] 实现类型包缓存机制
  - [ ] 实现热重载支持

- [ ] **4.1.1.3 内置类型包开发**
  - [ ] client（客户管理）
  - [ ] product（产品管理）
  - [ ] order（订单管理）
  - [ ] supplier（供应商管理）
  - [ ] project（项目管理）
  - [ ] article（文章）
  - [ ] note（笔记）
  - [ ] document（文档）

- [ ] **4.1.1.4 模板生成器**
  - [ ] 实现 frontmatter 自动填充
  - [ ] 实现预置数据块插入
  - [ ] 实现变量替换（{{title}}、{{today}}等）
  - [ ] 实现文件创建与保存

---

### Phase 4.1.2 - 新建文档流程（预计 1 周）

**目标**：实现基于类型包的新建文档用户界面

#### 任务清单

- [ ] **4.1.2.1 类型选择对话框**
  - [ ] 设计 UI 原型
  - [ ] 实现 NewDocumentDialog 组件
  - [ ] 实现类型包分类展示
  - [ ] 实现类型包搜索过滤

- [ ] **4.1.2.2 文档配置面板**
  - [ ] 实现文档名称输入
  - [ ] 实现保存位置选择
  - [ ] 实现预置数据块勾选
  - [ ] 实现高级选项（默认视图、能力配置）

- [ ] **4.1.2.3 文档创建流程**
  - [ ] 集成模板生成器
  - [ ] 实现文件创建 API
  - [ ] 实现创建后自动跳转
  - [ ] 实现错误处理与提示

---

### Phase 4.1.3 - 数据块资产池基础（预计 2 周）

**目标**：实现数据块资产池的基础架构

#### 任务清单

- [ ] **4.1.3.1 资产池数据结构**
  - [ ] 定义数据块 TypeScript 类型
  - [ ] 实现数据块 YAML 解析器
  - [ ] 实现兼容性检查器
  - [ ] 编写单元测试

- [ ] **4.1.3.2 个人资产池**
  - [ ] 实现个人资产池存储结构
  - [ ] 实现数据块 CRUD API
  - [ ] 实现收藏功能
  - [ ] 实现用户隔离

- [ ] **4.1.3.3 系统资产池**
  - [ ] 实现系统资产池存储结构
  - [ ] 实现公共数据块查询
  - [ ] 实现发布审核流程（可选）
  - [ ] 内置基础数据块
    - [ ] 联系人信息
    - [ ] 公司信息
    - [ ] 时间戳
    - [ ] 文件附件
    - [ ] 标签组
    - [ ] 跟进记录

- [ ] **4.1.3.4 兼容性过滤**
  - [ ] 实现白名单模式过滤
  - [ ] 实现黑名单模式过滤
  - [ ] 实现通用模式识别
  - [ ] 实现 API 层过滤

---

### Phase 4.1.4 - 资产池用户界面（预计 1.5 周）

**目标**：实现资产池的前端交互

#### 任务清单

- [ ] **4.1.4.1 资产池面板**
  - [ ] 设计 UI 原型
  - [ ] 实现 AssetPoolPanel 组件
  - [ ] 实现池切换（个人/系统/全球）
  - [ ] 实现搜索与过滤
  - [ ] 实现分类导航

- [ ] **4.1.4.2 数据块卡片**
  - [ ] 实现 BlockCard 组件
  - [ ] 显示名称、描述、评分、下载数
  - [ ] 实现悬停预览
  - [ ] 实现一键导入按钮

- [ ] **4.1.4.3 数据块预览**
  - [ ] 实现 BlockPreview 组件
  - [ ] 显示数据块代码
  - [ ] 显示示例数据渲染
  - [ ] 显示兼容类型列表

- [ ] **4.1.4.4 编辑器集成**
  - [ ] 在编辑器中添加"添加数据块"按钮
  - [ ] 实现数据块插入到光标位置
  - [ ] 实现插入动画效果

---

### Phase 4.1.5 - 数据块创建与发布（预计 1 周）

**目标**：实现用户创建和发布数据块

#### 任务清单

- [ ] **4.1.5.1 数据块编辑器**
  - [ ] 实现 BlockEditor 组件
  - [ ] 实现基础信息编辑
  - [ ] 实现兼容类型配置
  - [ ] 实现代码编辑器（语法高亮）
  - [ ] 实现实时预览

- [ ] **4.1.5.2 保存到个人池**
  - [ ] 实现保存 API
  - [ ] 实现重复检测
  - [ ] 实现版本管理

- [ ] **4.1.5.3 发布到系统池**
  - [ ] 实现发布确认对话框
  - [ ] 实现发布 API
  - [ ] 实现发布状态跟踪

- [ ] **4.1.5.4 基于现有数据块改造**
  - [ ] 实现"复制到个人池"功能
  - [ ] 实现数据块克隆
  - [ ] 保留原始来源信息

---

### Phase 4.1.6 - 全球资产池（预计 1.5 周）

**目标**：实现全球资产池同步

#### 任务清单

- [ ] **4.1.6.1 全球池 API 设计**
  - [ ] 设计 REST API 规范
  - [ ] 定义认证机制
  - [ ] 定义版本控制策略

- [ ] **4.1.6.2 同步服务**
  - [ ] 实现增量同步算法
  - [ ] 实现本地缓存
  - [ ] 实现定时同步任务
  - [ ] 实现手动同步触发

- [ ] **4.1.6.3 全球池浏览**
  - [ ] 实现全球池 Tab
  - [ ] 实现搜索（调用远程 API）
  - [ ] 实现排序（热门/最新/评分）
  - [ ] 实现下载到本地

- [ ] **4.1.6.4 发布到全球池**
  - [ ] 实现发布申请
  - [ ] 实现审核流程（后台）
  - [ ] 实现发布状态通知

---

### Phase 4.1.7 - 优化与完善（预计 1 周）

**目标**：性能优化、错误处理、文档完善

#### 任务清单

- [ ] **4.1.7.1 性能优化**
  - [ ] 类型包加载优化（懒加载）
  - [ ] 资产池查询优化（索引）
  - [ ] 前端组件优化（虚拟列表）
  - [ ] 缓存策略优化

- [ ] **4.1.7.2 错误处理**
  - [ ] 类型包加载失败处理
  - [ ] 网络错误处理（全球池）
  - [ ] 数据块导入失败处理
  - [ ] 用户友好错误提示

- [ ] **4.1.7.3 文档完善**
  - [ ] 类型包开发指南
  - [ ] 数据块创建指南
  - [ ] API 文档
  - [ ] 常见问题解答

- [ ] **4.1.7.4 测试**
  - [ ] 单元测试覆盖
  - [ ] 集成测试
  - [ ] E2E 测试
  - [ ] 性能测试

---

## 六、里程碑与时间线

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Phase 4.1 时间线                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   Week 1-2    │ Phase 4.1.1 - 类型包核心系统                                 │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                              │
│               │                                                              │
│   Week 3      │ Phase 4.1.2 - 新建文档流程                                   │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                             │
│               │                                                              │
│   Week 4-5    │ Phase 4.1.3 - 数据块资产池基础                               │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                              │
│               │                                                              │
│   Week 6-7    │ Phase 4.1.4 - 资产池用户界面                                 │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                      │
│               │                                                              │
│   Week 8      │ Phase 4.1.5 - 数据块创建与发布                               │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                             │
│               │                                                              │
│   Week 9-10   │ Phase 4.1.6 - 全球资产池                                     │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                      │
│               │                                                              │
│   Week 11     │ Phase 4.1.7 - 优化与完善                                     │
│   ─────────────────────────────────────────────                             │
│               │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                             │
│               │                                                              │
│   ─────────────────────────────────────────────────────────────────────     │
│               │                                                              │
│   总计: 约 11 周                                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 七、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 类型包结构过于复杂 | 用户难以理解和自定义 | 提供详细文档和可视化编辑器 |
| 全球资产池服务器成本 | 运营成本增加 | 初期使用 CDN 静态托管，后期考虑付费模式 |
| 数据块兼容性冲突 | 导入后数据块无法正常工作 | 严格的兼容性校验 + 预览机制 |
| 用户上传恶意内容 | 安全风险 | 审核机制 + 代码沙箱 + 举报功能 |
| 版本升级迁移 | 旧文档无法使用新类型包 | 版本兼容层 + 迁移工具 |

---

## 八、成功指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 新建文档步骤 | ≤ 3 步 | 选类型 → 填名称 → 创建 |
| 类型包覆盖率 | ≥ 80% | 常见业务场景有对应类型包 |
| 内置数据块数量 | ≥ 30 个 | 覆盖常见数据结构 |
| 用户自建数据块率 | ≥ 20% | 用户愿意创建自己的数据块 |
| 数据块复用率 | ≥ 50% | 超过一半的数据块被多次使用 |

---

## 附录

### A. 内置类型包清单

| 类型包 ID | 名称 | 分类 | 预置数据块 |
|-----------|------|------|-----------|
| client | 客户管理 | 业务 | 基本信息、联系方式、公司信息、跟进记录 |
| product | 产品管理 | 业务 | 产品信息、规格参数、价格配置、库存状态 |
| order | 订单管理 | 业务 | 订单信息、商品明细、收货地址、物流状态 |
| supplier | 供应商管理 | 业务 | 供应商信息、联系方式、合作记录 |
| project | 项目管理 | 业务 | 项目信息、里程碑、任务列表、成员 |
| employee | 员工管理 | 业务 | 员工信息、部门、职位、联系方式 |
| article | 文章 | 内容 | 摘要、正文、标签、封面 |
| note | 笔记 | 内容 | 内容、标签 |
| document | 文档 | 内容 | 目录、章节 |
| media | 媒体 | 内容 | 媒体信息、文件、缩略图 |

### B. 内置数据块清单

| 数据块 ID | 名称 | 兼容模式 | 适用类型 |
|-----------|------|----------|----------|
| basic_info | 基本信息 | 通用 | 所有 |
| contact_info | 联系方式 | 白名单 | client, supplier, employee |
| company_info | 公司信息 | 白名单 | client, supplier |
| follow_up | 跟进记录 | 白名单 | client, supplier, project |
| timestamp | 时间戳 | 通用 | 所有 |
| file_attachment | 文件附件 | 通用 | 所有 |
| tag_group | 标签组 | 通用 | 所有 |
| status_badge | 状态标记 | 通用 | 所有 |
| price_config | 价格配置 | 白名单 | product, order |
| address | 地址信息 | 白名单 | client, supplier, order |
| milestone | 里程碑 | 白名单 | project |
| task_list | 任务列表 | 白名单 | project, note |

---

*文档版本: 1.0*
*创建日期: 2026-01-04*
*作者: ATLAS Team*

